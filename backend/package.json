{
  "name": "backend",
  "private": true,
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "npx tsc",
    "db:setup": "npx ts-node db/setup.ts",
    "db:setup:prod": "NODE_ENV=production npx ts-node db/setup.ts",
    "db:verify": "npx ts-node db/setup.ts --verify",
    "db:config": "npx ts-node db/setup.ts --config-only",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "workspaces": [
    "crawler",
    "db"
  ],
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@types/express": "^5.0.3",
    "axios": "^1.10.0",
    "dotenv": "^16.5.0",
    "express": "^5.1.0",
    "mongodb": "^6.17.0"
  },
  "devDependencies": {
    "@types/dotenv": "^6.1.1",
    "@types/node": "^24.0.3",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.3"
  }
}
